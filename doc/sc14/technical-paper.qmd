---
title: "Jack mackerel MSE: technical information and work remaining"
format:
  html:
    number-sections: true
  pdf:
    documentclass: scrartcl
    fontsize: 9pt
    colorlinks: true
    geometry:
      - margin=0.65in
---

::: {.content-visible when-format="html"}
{{< include ../_includes/sprfmo-banner.qmd >}}

<div class="paper-title">Jack mackerel MSE: technical information and work remaining</div>

[Download the PDF version](technical-paper.pdf){.btn .btn-primary}
:::

## Scope

This paper allows a reviewer to find the CMP description, OM, performance
measure, and results that support the [full MSE
report](../evidence/sc14-mse-report.qmd) and the [SC14 questions and tasks
paper](decision-paper.qmd). It also distinguishes completed work from
scientific and practical work that remains.

## Which files control the results

The official set of files is organized in this order:

1. the saved source code, CMP settings, simulation settings, and complete runs;
2. the checked result files, including the 500-draw Slick file;
3. the CMP list, tables, figures, and scorecard inputs made from those results;
4. the explanation in the technical and main papers; and
5. the CMP and annual-use pages made from the same settings.

The current status is documented on the
[sources and changes page](../governance/traceability.qmd). The named release
also has an [automatic agreement report](../governance/release-check.qmd) with
the code versions, model runs, paper files, file fingerprints, numerical
comparisons, and products that are no longer current.

## Where to find the supporting information

| Topic | Current detailed source | Additional information needed |
|---|---|---|
| Development from SCW17 | [Current report: development](../evidence/sc14-mse-report.qmd#development-from-scw17-to-the-current-analysis) | **Completed for submission:** dated candidate translation table and decision status |
| OM set and simulation | [Operating models](../evidence/sc14-mse-report.qmd#operating-models-and-simulation-methods) | **Completed for submission:** definitions, analytical role, and proposed decision treatment; SC14 plausibility judgments remain |
| Index errors | [Generating OM data indices](../evidence/sc14-mse-report.qmd#generating-om-data-indices) | **Core documentation completed:** equation, values, coverage, overlap, gaps, and scope; additional index stress tests remain |
| CMP definitions | [CMP list](../cmp/index.qmd) and individual CMP pages | One table with names, roles, units, settings, and file version |
| Tuning | [Candidate procedures](../evidence/sc14-mse-report.qmd#candidate-management-procedures) | Exact quantity used for tuning, result for every CMP, uncertainty due to the number of runs, and effect of small changes to the trigger |
| Performance measures | [Performance evaluation](../evidence/sc14-mse-report.qmd#performance-evaluation-and-the-draft-scorecard) | **Submission framing completed:** calculations, periods, directions, weights, exclusions, and preliminary status; final measures await SC14 guidance |
| Alternative OMs | [Main findings](../evidence/sc14-mse-report.qmd#biological-requirements-and-the-complete-om-table) | **Completed for submission:** 112-row CMP-by-OM/stock table, including `om23` and both two-stock components |
| Banking and borrowing | [Current analysis](../evidence/sc14-mse-report.qmd#banking-borrowing-and-implementation-constraints) | Matched with/without comparison using identical OM draws before implementation conclusions |
| Annual calculation | [Annual workflow](../application/annual-workflow.qmd) and [input data](../application/input-data.qmd) | Complete instructions for calculating advice from every shortlisted CMP |
| Exceptional circumstances | [Current framework](../evidence/sc14-mse-report.qmd#exceptional-circumstances-and-annual-monitoring) | Indicators, thresholds, false alarms, missed detections, authority, and response options |

: Where to find the information needed for each decision. {#tbl-evidence-map}

## Results to report for each CMP and OM

The current decision table reports dynamic Kobe status, biomass and
overfishing risks, catch, and IACC for every CMP and OM or stock-component
combination. The complete downloadable table has 112 rows:

[Download the CMP-by-OM biological decision table](../data/candidates/candidate_biological_decision_table.csv).

The probabilities below are calculated over 2041--2050 from the checked
`SC14-MSE-2026-RC1` Slick object. `SB < 1` means spawning biomass below the
draw- and year-specific dynamic $SB_{\mathrm{MSY}}$; it is a candidate
diagnostic, not an agreed $B_{\mathrm{lim}}$ criterion. Catch is total catch for
one-stock OMs and component catch for two-stock OMs. None of the eight CMPs is
yet operationally ready for annual use.

```{=latex}
\begingroup\scriptsize
\setlength{\tabcolsep}{2.5pt}
```

```{r}
#| label: tbl-cmp-om-biological
#| tbl-cap: "Complete CMP-by-OM and stock-component biological decision table, 2041--2050."
#| echo: false

decision_table <- read.csv(
  "../data/candidates/candidate_biological_decision_table.csv",
  stringsAsFactors = FALSE, check.names = FALSE)
probability_columns <- c("p_dynamic_kobe_green",
  "p_sb_below_dynamic_sbmsy",
  "max_annual_p_sb_below_dynamic_sbmsy",
  "p_any_year_sb_below_dynamic_sbmsy", "p_f_above_fmsy")
decision_table[probability_columns] <- lapply(
  decision_table[probability_columns], function(x) sprintf("%.1f", 100 * x))
decision_table$median_long_term_catch <- sprintf("%.1f",
  decision_table$median_long_term_catch)
decision_table$median_long_term_iacc <- sprintf("%.1f",
  decision_table$median_long_term_iacc)
knitr::kable(decision_table[c("om", "stock", "cmp",
  "p_dynamic_kobe_green", "p_sb_below_dynamic_sbmsy",
  "max_annual_p_sb_below_dynamic_sbmsy",
  "p_any_year_sb_below_dynamic_sbmsy", "p_f_above_fmsy",
  "median_long_term_catch", "median_long_term_iacc")],
  col.names = c("OM", "Stock", "CMP", "Green %", "SB<1 %",
    "Max annual %", "Any year %", "F>1 %", "Catch", "IACC"),
  longtable = knitr::is_latex_output(), booktabs = TRUE)
```

```{=latex}
\endgroup
```

Before a final recommendation, the results package should add criteria based
on an agreed $B_{\mathrm{lim}}$, finalize near- and medium-term decision
periods, retain the low-catch and mean-reduction diagnostics, report catch-rate
or effort-feasibility measures and the frequency of a binding effort cap, and
complete the operational-readiness test.

The finite-run precision analysis also remains. It should distinguish
posterior/model uncertainty from future process and observation draws, confirm
the use of common draws among CMPs, provide uncertainty intervals for key
probabilities, and test whether conclusions change with more simulations or
different random seeds.

## Separate biological requirements from management preferences

The technical paper keeps three steps distinct:

1. **Minimum biological performance:** the requirements that determine whether
   a CMP can advance.
2. **Alternative assumptions:** performance and plausibility across alternative OMs,
   including component-specific outcomes.
3. **Management preferences:** trade-offs among candidates that meet the
   requirements in the first two steps.

The scorecard belongs only in the third step. Its measures, scaling, weights,
candidate set, and current comparison tests are saved in downloadable files.
The present worked examples show sensitivity to measures and weights and the
interactive application recalculates after candidate changes. Sensitivity to
OM choice and finite-run random variation remains work before a final
recommendation.

## Spatial structure and movement

The technical paper reports northern and southern components separately for
every two-stock OM and shows the regional indices used by the CMP. It explains:

- the fixed catch splits used to test different assumptions;
- the `split.is` method used in the CMP definitions; and
- any separate northern assessment or management process relevant to
  interpreting the results.

The allocation work is treated as complete for the present analysis. This
section documents what was tested and why; it does not open a new line of
allocation analysis. The author's response to the reviewer provides the
interpretation to accompany it.

## Instructions for annual calculation

Each shortlisted CMP will have one complete set of instructions containing
its indicator equation, HCR equation, allocation rule, parameters, data rules,
fallback rules, calculation sequence, and ECP interface. A saved retrospective
example will include the source data, intermediate values, unrounded advice,
annual-limit calculation, allocation, rounding, and final result.

An automatic test will run the example independently and compare every
intermediate and final value with saved expected results. Explanatory text
may explain the CMP but does not redefine its settings.

## Work remaining after SC14

Unless completed before submission, the paper records:

- final operational units, cut-offs, missing/revised-data rules, and rounding;
- matched banking-and-borrowing evaluation;
- advice-versus-realized-catch simulation or justification;
- simulation-precision analysis;
- final performance measures and agreed biological requirements;
- tests of whether the ECP detects problems, and rules for who decides what
  happens next; and
- an independent check that the annual calculation can be repeated.

Each item should have a named person or group responsible for it, a clear test
for completion, and a date for decision before a CMP is recommended for
adoption.
